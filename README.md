This is a react application setup using vite CLI. 

All the environment variable used in the workflow and package.json are binded with repositories secret keys.

Application is deployed to Vercel cloud.
